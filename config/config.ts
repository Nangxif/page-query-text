import { defineConfig } from '@umijs/max';

export default defineConfig({
  antd: {},
  access: {},
  model: {},
  initialState: {},
  request: {},
  mpa: {
    entry: {},
  },
  esbuildMinifyIIFE: true,
  npmClient: 'pnpm',
});
